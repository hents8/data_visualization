{# Expects: labels (array), data (array), id (string) #}

<div class="chart-container">
    <canvas id="{{ id }}"></canvas>
</div>

<script>
document.addEventListener('DOMContentLoaded', function () {
    const el = document.getElementById('{{ id }}');
    if (!el) return;

    const ctx = el.getContext('2d');

    const labels = {{ labels|json_encode|raw }};
    const data = {{ data|json_encode|raw }};

    new Chart(ctx, {
        type: 'doughnut',
        data: {
            labels: labels,
            datasets: [{
                data: data,
                backgroundColor: labels.map(label => getColor(label, 0.85)),
                borderColor: labels.map(label => getColor(label, 1)),
                borderWidth: 1
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: {
                legend: {
                    position: 'bottom',
                    labels: {
                        font: { size: 11 }
                    }
                },
                tooltip: {
                    callbacks: {
                        label: function (context) {
                            const total = context.dataset.data.reduce((a, b) => a + b, 0);
                            const value = context.raw;
                            const percent = total
                                ? ((value / total) * 100).toFixed(1)
                                : 0;
                            return `${context.label} : ${value} (${percent}%)`;
                        }
                    }
                }
            }
        }
    });
});
</script>
